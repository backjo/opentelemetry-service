receivers:
  jaeger:
  jaeger/customname:
    protocols:
      grpc:
        endpoint: "127.0.0.1:9876"
        disabled: true
      thrift-http:
        endpoint: "127.0.0.1:3456"

processors:
  exampleprocessor:

exporters:
  exampleexporter:

pipelines:
  traces:
   receivers: [jaeger]
   processors: [exampleprocessor]
   exporters: [exampleexporter]

